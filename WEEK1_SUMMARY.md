# Java AI 应用开发 · 第一周学习周报

**学习时间**：2026-06-09 ~ 2026-06-13
**开发者**：zzSuperMan97
**项目地址**：[GitHub Repository](https://github.com/zzSuperMan97/java-ai-learning)

##  本周目标
从零开始搭建 Spring Boot 环境，集成通义千问大模型 API，实现异步调用，并完成一个带 Web 界面的智能聊天助手。

## 📅 每日任务回顾

### Day 1 (周三): 环境准备与 Prompt 工程
- **任务**：注册阿里云账号，获取 DashScope API Key；学习 Prompt Engineering 基础。
- **成果**：掌握了 Prompt 两大原则（清晰指令、给模型思考时间）；成功申请 API Key。
- **知识点**：Prompt Engineering, Few-Shot Learning, Chain-of-Thought.

### Day 2 (周四): Spring Boot 初始化
- **任务**：安装 JDK 17 & Maven；创建 Spring Boot 项目；编写 Hello World 接口。
- **成果**：项目结构搭建完成，理解了 `@RestController` 和 `@GetMapping` 的作用。
- **知识点**：Spring Initializr, Maven 依赖管理, RESTful API 基础.

### Day 3 (周五): 集成通义千问 API
- **任务**：引入 `dashscope-sdk-java`；配置 API Key；实现同步调用 AI 接口。
- **成果**：通过浏览器访问 `/ai/chat?question=xxx` 成功获取 AI 回答。
- **知识点**：DashScope SDK, `@Value` 注入配置, HTTP Client 调用.

### Day 4 (周六): 异步编程优化
- **任务**：学习 `CompletableFuture`；将 AI 调用改为异步非阻塞模式。
- **成果**：实现了 `/ai/chat/async` 接口，提升了服务器在高并发下的吞吐量。
- **知识点**：JUC 并发包, `CompletableFuture.supplyAsync()`, 线程池基础.

### Day 5 (周日): Web 界面与工程化
- **任务**：引入 Thymeleaf 模板引擎；编写简易聊天前端页面；配置 Git 并上传 GitHub。
- **成果**：拥有了一个可视化的聊天窗口；学会了使用 `.gitignore` 保护敏感信息。
- **知识点**：Thymeleaf, View Resolver, Git 版本控制, GitHub 协作流程.

## 💡 核心知识点总结

### 1. Spring MVC 视图解析
- **@Controller vs @RestController**: 
    - `@RestController` = `@Controller` + `@ResponseBody`，适用于纯数据接口。
    - `@Controller` 配合 `return "pageName"` 可用于跳转 HTML 页面。
- **Thymeleaf 默认路径**: `src/main/resources/templates/`。

### 2. 异步调用 AI
- **为什么需要异步**: AI 推理耗时较长（秒级），异步可释放 Tomcat 线程，避免阻塞。
- **实现方式**: 使用 `CompletableFuture` 包裹耗时操作，Spring MVC 自动处理异步响应。

### 3. 安全规范
- **API Key 保护**: 严禁将 Key 硬编码在代码中或上传至公开仓库。
- **最佳实践**: 使用 `.gitignore` 忽略 `application.properties` 或通过环境变量注入。

## 🚀 遇到的问题与解决方案

| 问题描述 | 解决方案 |
| :--- | :--- |
| JDK 8 官方下载困难 | 改用 Adoptium Temurin JDK 17，适配 Spring Boot 3.x |
| GitHub 连接超时 (Port 443) | 配置 Git 全局代理 (`http.proxy`) 解决网络限制 |
| Thymeleaf 找不到模板 (404) | 确认 `chat.html` 位于 `resources/templates/` 目录下 |
| Controller 返回字符串被当作页面名 | 在数据接口方法上添加 `@ResponseBody` 注解 |

##  下周计划 (RAG 架构入门)
1. 学习向量数据库 (PostgreSQL + pgvector)。
2. 理解 Embedding (文本向量化) 原理。
3. 使用 Spring AI 实现基于私有文档的问答系统。

---
*Generated by QoderWork AI Assistant*
